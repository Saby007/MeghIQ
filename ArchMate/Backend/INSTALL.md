# Installation Instructions

Install dependencies using:

```powershell
pip install fastapi uvicorn[standard] pydantic python-dotenv azure-identity
pip install agent-framework --pre
```

Note: agent-framework requires --pre flag as it's in preview.
