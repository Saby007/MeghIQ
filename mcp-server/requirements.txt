# MeghIQ MCP Server — pinned production requirements.
#
# Pinning rationale: lower-bound ranges (>=) make builds non-reproducible
# because a fresh `pip install` may pull a never-tested newer version of
# any transitive dependency. Exact pins (==) ensure every deployment
# resolves to the same artefact set that passed CI.
#
# To regenerate against the currently deployed environment:
#     pip freeze > requirements.lock
# and copy the matching entries below.

mcp[cli]==1.23.0
pydantic==2.13.4

# Azure SDK
azure-identity==1.25.3
azure-mgmt-costmanagement==4.0.1
azure-mgmt-monitor==6.0.2
azure-mgmt-resource==23.4.0
azure-mgmt-resourcegraph==8.0.0

# HTTP / runtime
httpx==0.28.1
python-dotenv==1.2.2
python-dateutil==2.9.0.post0

# PDF report generation
fpdf2==2.8.4

# Web transport (streamable-http)
starlette==1.0.1
uvicorn==0.34.0

# XML hardening (XXE-safe parser)
defusedxml==0.7.1
